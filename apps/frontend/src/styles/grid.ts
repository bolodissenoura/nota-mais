import { styled } from "../../stitches.config";

export const FlexGrid = styled("div", {
  display: "flex",
  marginTop: "8px",
  justifyContent: "space-between",
  alignItems: "center",
  alignContent: "center",
});
